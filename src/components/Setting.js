import React from "react";
import styles from "../assets/styles/components/setting.less";

export const Setting = (props) => {
    return (
        <section className="section-setting">
            <header>
                <h1>Setting</h1>
            </header>

            <section className="main">
                test
            </section>
        </section>
    );
};
